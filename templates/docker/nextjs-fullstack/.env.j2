# Environment configuration for {{ workspace.name }}
# Generated by Panopticon template engine

COMPOSE_PROJECT_NAME={{ workspace.name }}

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:{{ computed.ports.database | default(variables.db_port) }}/{{ variables.database_name }}?schema=public

# App
NEXT_PUBLIC_APP_URL=https://{{ workspace.name }}.{{ docker.traefik.domain }}
