# Node.js Backend Dockerfile (monorepo)
FROM node:{{ variables.node_version }}-alpine

WORKDIR /app

# Copy root package.json for workspace setup
COPY package*.json ./
COPY backend/package*.json ./backend/

# Install dependencies
RUN npm ci

WORKDIR /app/backend

EXPOSE 3001

CMD ["npm", "run", "dev"]
