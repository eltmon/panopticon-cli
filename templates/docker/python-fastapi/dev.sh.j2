#!/bin/bash
# Development script for {{ workspace.name }}
# Generated by Panopticon template engine

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Colors
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

echo -e "${BLUE}Starting {{ variables.app_name | default('FastAPI') }} environment...${NC}"

if ! docker info > /dev/null 2>&1; then
    echo -e "${YELLOW}Docker is not running. Please start Docker first.${NC}"
    exit 1
fi

echo -e "${GREEN}Starting containers...${NC}"
docker compose up --build "$@"

echo -e "${GREEN}Development environment ready!${NC}"
echo ""
echo "API: https://api-{{ workspace.name }}.{{ docker.traefik.domain }}"
{% if variables.enable_frontend %}
echo "Frontend: https://{{ workspace.name }}.{{ docker.traefik.domain }}"
{% endif %}
echo "API Docs: https://api-{{ workspace.name }}.{{ docker.traefik.domain }}/docs"
