# Workspace: {{WORKSPACE_NAME}}

> Created: {{CREATED_AT}}
> Project: {{PROJECT_NAME}}

## Overview

This workspace was created for working on {{ISSUE_ID}}: {{ISSUE_TITLE}}.

## Directory Structure

```
{{WORKSPACE_PATH}}/
├── .planning/
│   ├── STATE.md        # Current progress and state
│   ├── CLAUDE.md       # Agent instructions
│   └── NOTES.md        # Working notes
├── .git/               # Git repository
└── [project files]     # Project source code
```

## Git Configuration

- **Branch**: `{{GIT_BRANCH}}`
- **Remote**: `{{GIT_REMOTE}}`
- **Base Branch**: `{{BASE_BRANCH}}`

## Environment

### Environment Variables
```bash
# Required environment variables for this workspace
{{ENV_VARS}}
```

### Services
| Service | Status | Port | Notes |
|---------|--------|------|-------|
{{SERVICES}}

## Quick Commands

### Development
```bash
# Start development server
{{DEV_COMMAND}}

# Run tests
{{TEST_COMMAND}}

# Build
{{BUILD_COMMAND}}
```

### Git Operations
```bash
# Check status
git status

# Create commit
git add -A && git commit -m "description"

# Push changes
git push origin {{GIT_BRANCH}}
```

## Related Resources

- **Issue URL**: {{ISSUE_URL}}
- **Repository**: {{REPO_URL}}
- **Documentation**: {{DOCS_URL}}

## Agent Access

This workspace is managed by Panopticon agents. To interact:

```bash
# Check agent status
pan work status

# Send message to agent
pan work tell {{ISSUE_ID}} "Your message"

# View agent logs
tmux capture-pane -t agent-{{ISSUE_ID}} -p
```

## Notes

{{NOTES}}
